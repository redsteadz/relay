{
  "name": "@relay/pipeline",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "corepack pnpm build:dependencies && wrangler deploy --dry-run --outdir dist",
    "build:dependencies": "corepack pnpm --filter \"@relay/pipeline^...\" build",
    "clean": "rm -rf .wrangler dist coverage",
    "predev": "corepack pnpm build:dependencies",
    "deploy:hosted": "corepack pnpm build:dependencies && wrangler deploy",
    "dev": "wrangler dev --local",
    "lint": "eslint src tests",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@relay/contracts": "workspace:*",
    "@relay/crypto": "workspace:*",
    "@relay/domain": "workspace:*",
    "@relay/observability": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260823.1",
    "@relay/config": "workspace:*",
    "wrangler": "4.125.0"
  }
}
